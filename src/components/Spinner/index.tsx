import React, { FC } from 'react';

import { Wrapper, Element } from './styles';

const Spinner: FC = () => {
  return (
    <Wrapper>
      <Element />
      <Element />
    </Wrapper>
  );
};

export default Spinner;
